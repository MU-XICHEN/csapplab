思路是：
- 在栈上插入代码，并使其执行
在 stack 的地址不变，且允许在 stack 上执行（%rip 可以指向栈上地址）的环境下
重新返回地址，使其指向 当前rsp + 8 的位置，即ret之后，rip 和 rsp 指向同一个地址
该地址及之后便是插入代码部分

- 插入代码的工作
    - 申请需要的变量空间，并记录位置（地址
    - 最后将目标地址压入栈，并 retq 跳转执行

- 为什么不用 jmp or call
    jmp 和 call 的机器指令本身会携带偏移量，而当前是在 ip 指向 stack，计算偏移量很复杂（代码变动就要重新计算，且偏移量很大）